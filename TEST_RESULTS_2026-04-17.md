# 知微系统测试结果报告

文稿整理时间：2026-04-17  
实际测试时间：2026-04-16  
时区：Asia/Shanghai  
测试对象：`/Users/shennaitong/Desktop/ai-gf-zhiwei`

## 1. 测试目标

本轮把“知微”按待上线产品进行了一次偏产品化、偏运维化、偏代码健康度的系统测试，覆盖以下方向：

1. 外观设计与信息架构
2. Dashboard 交互逻辑与可用性
3. 核心接口与权限边界
4. 后台任务、记忆流转与数据一致性
5. 搜索、附件、性能、健康检查与观测能力
6. 代码可维护性、复杂度与测试覆盖缺口

本轮没有修改业务代码，主要完成了代码审查、脚本执行、浏览器补测与接口复现。

## 2. 项目形态判断

当前项目是一个以 `Discord DM + SQLite + Dashboard` 为核心形态的长期陪伴系统，包含以下主要模块：

1. Discord Bot 消息收发
2. 长短期记忆与结构化事实
3. 搜索式回复、绘图式回复、附件理解
4. 后台任务与主动消息
5. FastAPI Dashboard
6. SQLite 持久化与观测面板

## 3. 已执行的自动化验证

本轮实际运行并确认结果的校验如下：

1. `python3 -m compileall src scripts`
   结果：通过
2. `python3 scripts/verify_product.py`
   结果：通过
3. `python3 scripts/dashboard_contracts.py`
   结果：通过
4. `python3 scripts/dashboard_e2e.py`
   结果：通过
5. `python3 scripts/dashboard_visual_regression.py`
   结果：通过

结论：

1. 当前仓库对主路径已经有一定自测能力。
2. 现有脚本主要证明“主链路可用”，不能证明“边界场景健壮”。
3. 若只看自动化结果，会低估事务一致性、范围语义和交互细节上的风险。

## 4. 补充人工与半自动复现结果

除已有脚本外，本轮还补做了以下复现：

1. 用 Playwright 复现错误登录流程，观察登录失败后的页面行为。
2. 用 Playwright 复现 Dashboard 顶部全局搜索框按 Enter 的行为。
3. 用 TestClient 复现候选记忆批量审核中途失败时的数据落库状态。
4. 用 TestClient 对比 `scope_mode=active` 与 `scope_mode=all` 的性能页返回值，验证范围语义是否一致。

补测结论：

1. 错误登录时前端会被 401 直接重定向回 `/login`，登录错误文案没有稳定显示。
2. 顶部搜索框按 Enter 不会触发搜索，只能点按钮。
3. 批量审核接口会在中途报错前部分提交，存在半成功状态。
4. 性能页在 active scope 模式下混入全局 experience 数据，范围语义不一致。

## 5. 本轮确认的高优先级问题

以下 5 项是本轮已明确确认、且已经能用实际测试或源码路径支撑的问题。

### P0-1 批量审核存在部分提交风险

- 位置：`src/dashboard/server.py:1476-1491`
- 问题：`/api/candidates/batch-review` 逐条执行 approve/reject，没有外层事务。
- 复现：向接口提交一个真实候选 id 和一个不存在的候选 id，接口返回 `404`，但第一条候选已经从 `pending -> approved`。
- 风险：运营会误以为这次操作整体失败，但实际已经发生部分不可逆状态变更。

### P0-2 场景分支把 token 上限重新放大到 8k-12k

- 位置：`src/services/reply_service.py:315-333`
- 问题：风格校准器原本把 `max_tokens` 控在约 `560-1100`，但 `_apply_plan_to_style()` 又把常见场景直接抬到 `8192-12288`。
- 风险：学习辅导、情绪安慰等高频场景会重新引入超长回复、尾延迟飙升与成本失控。

### P1-1 Dashboard 来源 IP 可被伪造

- 位置：`src/dashboard/security.py:44-49`
- 问题：`request_source_ip()` 无条件信任 `x-forwarded-for` 的第一跳。
- 风险：在没有可信反向代理兜底时，登录限流、锁定与审计里的来源 IP 都可能被伪造。

### P1-2 任意 401 都会触发跳转，导致登录失败信息丢失

- 位置：`src/dashboard/static/dashboard.js:103-105`
- 问题：`apiFetch()` 遇到任何 `401` 都直接 `window.location.href = "/login"`。
- 复现：浏览器中输入错误密码后，页面回到 `/login`，`#login-error` 为空。
- 风险：用户无法得到稳定错误反馈，更容易反复尝试并触发锁定。

### P1-3 性能页 active scope 与 global metrics 混用

- 位置：`src/dashboard/server.py:1212-1217`
- 问题：`turns` 按 active conversation 过滤，但 `performance_summary` 和 `experience_summary` 仍走全局聚合。
- 复现：用 TestClient 比较 `scope_mode=active` 与 `scope_mode=all`，experience sample 保持一致。
- 风险：运营会误判当前 scope 的实际表现。

## 6. 其他已观察到的重要现象

这些问题未列入上面 5 条核心 finding，但已经在本轮测试中看到，建议一并进入修复排期。

1. 顶部全局搜索框按 Enter 无效，鼠标点击和键盘行为不一致。
2. 健康页将 `healthy` 状态按危险色渲染，因为前端只把 `ok` 当成功态。
3. Dashboard 现有“全局搜索”并不是真正的全局统一范围，不同数据组的过滤语义不一致。
4. `build_dashboard_app()` 与 `dashboard.js` 已明显过大，后续改动成本会越来越高。
5. 破坏性动作基本依赖后端 409/422 提示，前端缺少更友好的禁用、确认和状态预判。

## 7. 分级优化建议总表

本轮共输出不少于 100 条建议，按 `P0 / P1 / P2` 分级如下：

- P0：8 条
- P1：40 条
- P2：52 条
- 合计：100 条

## 8. P0 建议

1. 把候选记忆批量审核改成整批事务提交，出现任意失败时必须整批回滚。
2. 修正 `_apply_plan_to_style()` 对 `max_tokens` 的异常放大，恢复到受控区间。
3. 不要无条件信任 `X-Forwarded-For`，需要显式 trusted proxy 策略。
4. 给 `CompanionService.handle_message()` 增加按会话串行化控制，防止同一用户连续消息并发踩状态。
5. 为附件读取增加大小上限、类型拦截和更稳妥的处理策略，避免整文件直接读入内存。
6. 修掉登录失败时 401 直接跳转导致的错误提示丢失问题。
7. 将后台任务 dedupe 从“先查再插”升级为数据库层原子约束或等价能力。
8. 为 Discord 消息落库增加幂等保护，避免消息重放导致重复 turn 和重复记忆。

## 9. P1 建议

1. 健康状态徽标应把 `healthy` 渲染为成功态，而不是危险态。
2. `scope_mode=active` 下，性能页和 experience 页都应按 active scope 聚合。
3. 总览页里的用户级、会话级、系统级指标要统一范围语义。
4. Scope 摘要里的 `active_memories` 统计口径要和文案保持一致。
5. “全局搜索”需要要么真正全局，要么改名为“当前范围搜索”。
6. 搜索结果需要能直接跳转到对应对象，而不只是只读列表。
7. Search 健康检查应补真实可达性探测，不能只显示“功能已启用”。
8. DuckDuckGo HTML 解析需要 fixture 回归测试和结构变化兜底策略。
9. Origin/Referer 校验需要补反向代理与自定义域名场景支持。
10. CSP 里仍保留 `unsafe-inline`，建议逐步收紧。
11. 生产环境下应避免静默生成 `DASHBOARD_SESSION_SECRET`。
12. 改密成功后建议刷新 session 与 CSRF token。
13. 顶部搜索框按 Enter 应触发搜索。
14. 各类 filter 表单建议补显式 label，提升可访问性。
15. 候选记忆已选项在切筛选、切 scope、刷新后应明确清空或提示保留。
16. 单条审核与批量审核共用备注输入框，容易误带旧备注。
17. 批量按钮在未选项时应禁用。
18. 归档、批量审核、任务取消、错误归档等破坏性动作建议增加确认步骤。
19. 任务页按钮应根据任务状态做前置禁用，而不是依赖后端报错。
20. 登录与写操作应增加 loading / disabled 态。
21. 成功提示条应避免多次 `setTimeout` 带来的覆盖竞态。
22. 自动刷新应避免请求叠加。
23. 切 tab 时应避免旧请求晚到覆盖新面板数据。
24. 多个面板直接渲染大块 JSON，可读性与性能都不理想。
25. `performance.scope_mode` 已有后端能力，但 UI 没有对应控件。
26. `health.history_limit` 已有状态位，但 UI 未暴露。
27. `/api/modes` 已存在，但 Dashboard 没有模式控制入口。
28. 日志“下载”动作的语义应明确为下载当前窗口切片，而不是全量日志。
29. 日志行号不是原文件真实行号，建议显式说明。
30. `tail_log_file()` 每次从头读取完整文件，日志大时会拖慢接口。
31. 安全面板、审计面板和日志面板需要更结构化的展示，而不是原始 JSON 堆叠。
32. `status`、`component`、`task_type` 等筛选建议改成枚举型控件。
33. 不同页面中的状态颜色映射应统一。
34. 顶部搜索入口和“全局搜索”页应做双向同步。
35. Scope 切换建议增加“已修改未应用”的可见提示。
36. 强制改密状态下，前端应显式禁用其他写操作按钮。
37. 候选 dedupe 视图不应直接暴露难理解的签名值，应转换成更可读的信息。
38. Planner 场景优先级应重新评估，避免“情绪+学习”混合输入被错误路由。
39. 回复后处理和 Style Guard 不应过度抹平结构化输出。
40. 用户消息 metadata 里不宜长期保留原始附件 URL。

## 10. P2 建议

1. 18 个 tab 已超过瞬时可扫读上限，建议分组导航。
2. 页面中的中英术语风格应统一。
3. 顶部工具条应拆分高频操作与偏好设置。
4. 每个面板可以增加“这个面板解决什么问题”的说明。
5. 常用 Scope、Error、Task 适合支持 pin。
6. 空状态应提供下一步动作，而不是只写“暂无”。
7. 常用筛选条件可支持保存视图。
8. 当前 tab、筛选、页码建议进 URL。
9. 增加键盘快捷键，例如 `/` 聚焦搜索。
10. 补 `:focus-visible` 样式。
11. 补 `prefers-reduced-motion` 支持。
12. 按钮需要更清晰的 active / pressed 反馈。
13. 字体栈建议自托管或提供更稳定的回退。
14. `backdrop-filter` 需要无该能力时的稳定降级。
15. 移动端表格建议做卡片化布局。
16. 长表格建议支持 sticky 表头。
17. 候选页的“全选”应明确是当前页还是全结果。
18. 日志页适合把级别、request id、turn id 做成结构化信息。
19. 性能页更适合图表化而不是原始 JSON。
20. 健康页可增加阈值线与连续失败提示。
21. 安全面板可单独展示高风险来源列表。
22. 审计页应增加 actor、scope、日期范围筛选。
23. 主动消息页可增加接受率与冷响应率趋势图。
24. 附件页应更明确标出“截断”“解析失败”“未存正文”等状态。
25. 快照页可把新增、删除、修改拆开展示。
26. Turn 页可把 stage latency 做成横向对比条。
27. 总览卡片建议显式标注统计口径。
28. 页面上建议显式展示时区信息。
29. 登录被锁定时最好显示剩余锁定时间。
30. “显示/隐藏密码”按钮可补 `aria-pressed`。
31. 各类排障主键适合做一键复制。
32. 搜索结果应支持 drilldown。
33. 搜索应支持“当前 scope / 当前 user / 全库”三种范围切换。
34. UI 上应更明确地区分用户级 scope 和会话级 scope。
35. 总览页建议直接展示 backlog、running、retrying 任务概况。
36. Error 面板建议支持 fingerprint 聚合。
37. Task / Error 状态变化最好记录操作者与原因。
38. Scope 摘要可展示 proactive opt-in 状态。
39. 对超大附件和不支持类型应提供更明确的用户侧反馈。
40. 附件能力可扩展更多文档类型或明确写出边界。
41. 搜索结果 URL 建议限制在安全 scheme。
42. Search 与 LLM 客户端适合补重试、退避和熔断。
43. 网络超时建议拆分 connect/read/write，而不是单一总超时。
44. 同步 SQLite 访问在量大时会成为瓶颈，后续可考虑异步化或专用 DB worker。
45. Dashboard 高频查询可以继续补复合索引。
46. `record_memory_hits()` 等循环写操作适合合并事务。
47. LLM 提取结果建议在落库前先做 schema 校验。
48. `confidence`、`importance`、`weight`、`expires_in_minutes` 应统一做 clamp。
49. 需要补“错误登录原地提示”的自动化测试。
50. 需要补“顶部搜索按 Enter 触发”的自动化测试。
51. 需要补“批量审核失败全回滚”的自动化测试。
52. `build_dashboard_app()` 与 `dashboard.js` 建议拆模块并集中管理枚举与文案。

## 11. 代码结构观察

当前代码在“功能齐全”的同时，也出现了明显的可维护性压力：

1. `src/dashboard/server.py` 体量很大，`build_dashboard_app()` 过长。
2. `src/dashboard/static/dashboard.js` 体量很大，渲染与状态管理耦合紧。
3. `src/services/companion_service.py` 的 `handle_message()` 逻辑过重。
4. 项目缺少更常规的单元测试目录与持续集成配置。
5. 当前回归更偏脚本式，缺少更细粒度的行为测试与边界测试。

## 12. 当前总体结论

可以把当前项目判断为：

1. 核心路径可运行，且已有一定自动化保障。
2. 体系结构已经从“原型”走向“产品骨架”，基础能力不弱。
3. 但在事务一致性、范围语义、交互反馈、边界场景和代码拆分上，还有一轮非常值得做的收敛。
4. 如果继续迭代，优先级应从“继续堆功能”转向“修关键一致性问题 + 收紧交互闭环 + 降低维护复杂度”。

## 13. 建议的修复顺序

建议优先级如下：

1. 先修批量审核事务一致性。
2. 立即收回异常放大的 `max_tokens`。
3. 修登录失败提示丢失与来源 IP 信任问题。
4. 修性能页 active scope 统计口径错误。
5. 再处理 Dashboard 交互闭环、状态提示与测试补齐。
6. 最后推进前端模块化、结构化展示与持续集成建设。

